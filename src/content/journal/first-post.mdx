---
title: First post title
description: >-
  One or two sentences describing this post. This is what shows in the post
  list and in search results.
pubDate: "2026-01-15"
tags:
  - compliance
draft: false
---

This is a placeholder post so the journal layout can be seen and tested. The
journal is switched off until `features.journal` is set to true in
`src/content/site.ts`, so nothing here is public yet.

Replace this file, or delete it and copy `_template.mdx` to start a real post.

## A section break

The heading above renders as a short centered rule rather than a bold line, so
a long post keeps reading as a single document rather than a stack of blocks.

> A pull quote is set in the display face at subhead size and indented to the
> rule.

Body text is set in Literata at a single 68 character measure, which is loaded
only on journal routes so the CV pages stay on two families.
